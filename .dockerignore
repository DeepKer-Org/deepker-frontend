# Node modules (exclude to avoid copying host-specific dependencies)
node_modules/

# Build output (Next.js build artifacts, excluded as they are generated in the build stage)
.next/
out/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment files (exclude sensitive files, only include what's needed via COPY)
.env
.env.local
.env.development
.env.test

# Dependency lock files from other package managers (if you only use npm)
yarn.lock
pnpm-lock.yaml

# TypeScript build cache
*.tsbuildinfo

# Linting and testing artifacts
coverage/
*.lcov

# Temporary files
*.tmp
*.bak
*.swp

# IDE and editor files
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# macOS system files
.DS_Store

# Git-related files
.git/
.gitignore

# CI/CD and other configuration files
.github/
.gitlab-ci.yml
.circleci/
Dockerfile
.dockerignore